"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _default = (tableName, columns) => {
  const firstIdColumnNames = columns.map(column => {
    return column.name;
  }).filter(columnName => {
    return columnName.endsWith('_id');
  }).map(columnName => {
    return columnName.slice(0, -3);
  }).slice(0, 2).sort();

  if (firstIdColumnNames.length < 2) {
    return false;
  }

  return firstIdColumnNames.join('_') === tableName;
};

exports.default = _default;
//# sourceMappingURL=isJoiningTable.js.map