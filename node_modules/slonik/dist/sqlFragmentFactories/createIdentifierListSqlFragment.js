"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _utilities = require("../utilities");

const createIdentifierListSqlFragment = token => {
  const sql = token.identifiers.map(identifier => {
    if (Array.isArray(identifier)) {
      return identifier.map(identifierName => {
        if (typeof identifierName !== 'string') {
          throw new TypeError('Identifier name must be a string.');
        }

        return (0, _utilities.escapeIdentifier)(identifierName);
      }).join('.');
    }

    return identifier.identifier.map(identifierName => {
      if (typeof identifierName !== 'string') {
        throw new TypeError('Identifier name must be a string.');
      }

      return (0, _utilities.escapeIdentifier)(identifierName);
    }).join('.') + ' ' + (0, _utilities.escapeIdentifier)(identifier.alias);
  }).join(', ');
  return {
    sql,
    values: []
  };
};

var _default = createIdentifierListSqlFragment;
exports.default = _default;
//# sourceMappingURL=createIdentifierListSqlFragment.js.map